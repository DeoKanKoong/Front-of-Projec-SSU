<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Article Details</title>
    <link rel="stylesheet" href="/path/to/bootstrap.css">
</head>
<body>

<header>
    <!-- Include header layout -->
</header>

<main class="container">
    <h1>Article Details</h1>

    <table class="table">
        <thead>
        <tr>
            <th scope="col">제목</th>
            <th scope="col">모집인원</th>
            <th scope="col">마감기한</th>
            <th scope="col">조건</th>
            <th scope="col">우대사항</th>
            <th scope="col">분야</th>
            <th scope="col">대표사진</th>
            <th scope="col">내용</th>
        </tr>
        </thead>
        <tbody>
        <tr>
            <td th:text="${article.title}">제목</td>
            <td th:text="${article.numberOfPeople}">모집인원</td>
            <td th:text="${#dates.format(article.deadline, 'yyyy-MM-dd')}">마감기한</td>
            <td th:text="${article.condition}">Condition</td>
            <td th:text="${article.preferentialTreatment}">Preferential Treatment</td>
            <td th:text="${article.field}">Field</td>
            <td>
                <img th:src="@{${article.representativeImage}}" alt="Representative Image" style="max-width: 200px;">
            </td>
            <td th:text="${article.content}">Content</td>
        </tr>
        </tbody>
    </table>

    <a th:href="@{/articles/{id}/edit(id=${article.id})}" class="btn btn-primary">수정</a>
    <a th:href="@{/articles/{id}/delete(id=${article.id})}" class="btn btn-danger">삭제</a>
    <a href="/articles">게시판으로 이동</a>

    <div>
        <h2>댓글</h2>
        <div th:each="comment : ${commentDtos}">
            <p><strong th:text="${comment.author}">Author</strong>: <span th:text="${comment.content}">Content</span></p>
        </div>
    </div>
</main>

<footer>
    <!-- Include footer layout -->
</footer>

</body>
</html>
